# 自炊アプリ「jisui」設計書

Claude Code にこのファイルを読ませて実装を始めるための設計書。
現行の Cowork 運用(jisui.db + チャット)で検証済みのルールをそのまま持ち込む。

---

## 1. 何を解決するのか

**解決したい問題はひとつだけ**: スーパーで買い物リストにチェックしても記録されない。

現状はHTMLの静的ファイルを配っているため、チェックは見た目だけで消える。帰宅後にレシート写真を送って記録する運用は成立しているが、以下が積み残しになっている。

- 買い物中のチェックがどこにも残らない(妻と手分けすると買い忘れ・重複が起きる)
- 在庫を直したいとき、チャットを開いて文章で伝えるしかない
- Cowork がパソコンの jisui.db に触れるのは**デスクトップアプリが開いている間だけ**。閉じていると書き込みが保留になる(実際に発生済み)

**この3つを、データをクラウドに置くことで同時に解く。**

### やらないこと(非目標)

- レシピ検索サービスや献立の自動生成エンジンを作ること。AIはClaude APIに任せる
- 資産運用の管理アプリを作ること。資産・負債・給与・投資方針は手元の kakeibo.db + Cowork のまま(家計簿は「支出」だけ組み込む。5-2参照)
- 汎用的な「みんなが使える自炊アプリ」にすること。夫婦2人の専用アプリとして最小に保つ

---

## 2. 方式(結論)

| 層 | 採用 | 理由 |
|---|---|---|
| フロント | **Next.js (App Router) + TypeScript + Tailwind** | PWA化してスマホのホーム画面に置ける。Claude Codeが最も書き慣れている |
| ホスティング | **Vercel** | git push で自動デプロイ。個人利用なら無料枠で足りる想定(最新の条件は要確認) |
| DB / 認証 / 保存 | **Supabase** | Postgres + 認証 + リアルタイム + 画像ストレージが1つで揃う |
| AI | **Claude API**(Next.js の Route Handler 経由) | レシート読み取り・献立提案・レシピ生成。APIキーはサーバ側のみ |

### なぜ Supabase か

2人で使うことが決め手になった。**リアルタイム同期**(妻がチェックしたら自分の画面も即更新)、**認証**、**レシート画像の保存**が最初から揃っている。SQLite互換のTursoも候補だったが、現行スキーマは小さいのでPostgresへの移植コストは小さく(移植済みSQLを同梱)、リアルタイムと画像保存の価値が上回る。

### なぜ PWA か(ネイティブアプリではなく)

App Store / Google Play への申請・審査・更新の手間がゼロ。夫婦2人ならURLを共有してホーム画面に追加するだけで済む。**スーパーの地下は電波が悪いことが多い**ため、オフライン対応(後述)のほうがネイティブ化より体感の使いやすさに効く。

---

## 3. 使いやすさのための設計(ここが本体)

### 3-1. オフライン優先(最重要)

スーパーで電波が切れてもチェックできなければ意味がない。

- チェック操作は**まずローカル(IndexedDB)に即時反映**し、UIは待たずに更新する(楽観的更新)
- オンラインになった時点でSupabaseへ同期。同期待ちの件数を画面上部に小さく出す
- 競合(2人が同じ品目を同時に触る)は「**チェック済みが勝つ**」で解決。買い物では二重購入の防止が最優先で、外したい場合は手動で戻せばよい
- Service Worker でアプリ本体と最新の買い物リストをキャッシュし、圏外でも起動する

### 3-2. 買い物リスト画面(ホーム画面にする)

アプリを開いたら最初にこれが出る。片手で、見ずに近い感覚で押せることを最優先にする。

- **タップ領域は行全体**(高さ56px以上)。小さなチェックボックスを狙わせない
- **売り場順**にセクション分け(野菜 → 肉・魚 → 乳製品・卵・豆腐 → 加工品 → 調味料 → 冷凍)。並べ替えは `sort_order` で持つ
- チェックすると打ち消し線+セクション内で下に沈む。誤タップはもう一度タップで戻せる
- 上部に「**残り 12 / 17**」の進捗。セール枠は本数に含めない
- 各品目に「何のために買うか」(例: 8/11 麻婆豆腐)を小さく添える。代替品を選ぶ判断ができる
- **セール枠**は黄色の別セクション。安ければ買ってよい候補。買ったらチェックせず「追加」で在庫に入れる
- 誰がチェックしたかを薄く表示(2人で手分けした時に効く)

### 3-3. 在庫画面

- 冷蔵 / 冷凍 / 常温 のタブ。各食材に「使う予定の献立」を併記
- **数量の増減は +/- ボタン**。キーボードを出さない
- 「使い切った」を1タップで削除。長押しで数量編集
- 期限が近いものを上部に赤く出す

### 3-4. 記録の入口は2つ、DBは1つ

アプリを作ってもチャットは捨てない。**得意なことが違うため。**

| やること | どこで | なぜ |
|---|---|---|
| 買い物中のチェック | **アプリ** | 片手で1タップ。オフラインでも動く |
| 在庫の手直し | **アプリ** | 冷蔵庫を開けながら +/- を押すだけ |
| レシート20点の一括登録 | **チャット** | 写真1枚で金額込み全件が入る。フォーム入力より圧倒的に速い |
| 献立の相談・レシピ生成 | **チャット** | 会話で条件を足していける(「今日はさっぱり系」など) |
| 好み・NG食材の登録 | **チャット** | 会話の流れで自然に拾える |

そのために **Cowork 側の jisui スキルを、SQLite ではなく Supabase を読み書きするように書き換える**(REST API を叩く)。これで**パソコンが閉じていてもチャットから記録できる**ようになり、現状の弱点も同時に消える。

---

## 4. データ設計

同梱の `schema.sql`(Supabase / PostgreSQL用)をそのまま実行する。**PostgreSQL 16 で実行検証済み。**

現行 jisui.db からの主な変更点:

- 全テーブルに `household_id` を追加し、**行レベルセキュリティ(RLS)**で自分の世帯のデータだけ見えるようにした
- `recipes.card_path`(ファイルパス)を **`card_md`(Markdown本文)** に変更。ファイルシステムに依存しなくなる
- `shopping_list` に `sort_order`(売り場順)、`checked_by` / `checked_at`(誰がいつチェックしたか)を追加
- 真偽値を 0/1 から boolean に、区分値に CHECK 制約を追加
- `shopping_list` と `inventory` をリアルタイム配信対象に登録

既存データの移行は `seed.sql` を実行するだけ(**こちらも実行検証済み**)。器具11点・常備品32点・好み5件・在庫18点・レシピ6品(カード本文込み)・材料39行・献立6件・買い物リスト6件が入る。

---

## 5. AI機能(Claude API)

すべて Next.js の Route Handler(サーバ側)から呼ぶ。APIキーはクライアントに出さない。

| 機能 | 入力 | 出力 |
|---|---|---|
| レシート読み取り | 撮影画像 | 品目・数量・金額の候補一覧 → 確認画面で修正して在庫へ登録 |
| 献立提案 | 在庫・好み・直近2週間の調理履歴・登録器具 | 指定日数分の献立案 |
| 買い物リスト生成 | 承認された献立 − 在庫 | 売り場順のリスト + セール枠 |
| レシピ生成 | 料理名・在庫・器具 | 「なぜその工程が必要か」「栄養メモ」「失敗しやすいポイント」つきのカード |

**提案の作法は現行スキル(SKILL.md)の原則をそのままシステムプロンプトに入れる。** 特に:

- `preferences` の「苦手」は絶対に使わない(現在: トマト、バナナ)
- `equipment` にない器具を使うレシピは出さない
- `pantry` にある調味料は買い物リストに載せない。米は絶対に載せない(実家から貰うため)
- レシピは初心者向けに「なぜ」まで解説する。「適量」「少々」は定義なしに使わない
- 妻は少食だが増量方針 → 量ではなくカロリー密度で調整する

---

## 5-2. 家計簿の組み込み(支出のみ)

### 何を移し、何を移さないか

**移す**: 取引履歴(transactions)と分類辞書(店名キーワード → 費目)。
**移さない**: 資産・負債・住宅ローン残高・給与・俸給表・投資方針(IPS)。これらは手元の `kakeibo.db` と Cowork のまま。

理由は2つ。ひとつは**機密度**で、ローン残高や給与情報をクラウドに置くのは、買い物リストを置くのとはリスクの水準が違う。もうひとつは**必要性**で、資産推移は月1回デスクで見るものであり、スマホでの即時性もリアルタイム同期も要らない。既存の `dashboard.html` と `kakeibo_mobile.html` で足りている。

### 移すと何が良くなるのか

**レシート1枚で「在庫」と「食費」が同時に記録される。** 現状はjisui.dbとkakeibo.dbが別物なので、同じレシートを2つの仕組みに別々に入れており、しかも「カード明細CSVを取り込むと二重計上になる」という危険が残ったままになっている(2026-08-06の取引に実際にフラグが立っている)。1つのDBに入れば、レシートを撮った時点で品目は在庫へ、合計は支出へ、同じトランザクションとして記録できる。

**「1食あたりコスト」が出せる。** 食費 ÷ その月の調理回数。自炊が家計にどう効いているかが数字で見える。これは両方のデータが揃って初めて計算できる指標で、同梱の `schema_kakeibo.sql` にビュー `v_cost_per_meal` として用意してある。

**買い物中に今月の食費が見える。** 予算感を持ってセール枠を判断できる。

### カード明細CSVの扱い

カード明細CSVはパソコンにダウンロードするものなので、**取り込みはCowork(チャット)に残す**のが合理的。アプリ側はレシート入力に専念する。重複照合(レシートで入れた行とカード明細の行が同じ買い物を指す問題)は `needs_review` フラグと `dedup_hash` で管理し、Cowork側の取り込み時に照合する。

### 追加のセキュリティ配慮

- 取引テーブルもRLSで世帯を分離(`schema_kakeibo.sql` に設定済み)
- **口座番号・カード番号は保存しない**(現行の家計簿スキルの禁止事項をそのまま踏襲)
- レシート画像は Supabase Storage の**非公開バケット**に置き、署名付きURLでのみ読む

---

## 6. 作る順番

### フェーズ1: 買い物リスト(まずここまでで一度リリースする)

1. Supabase プロジェクト作成 → `schema.sql` → `seed.sql` を実行
2. Supabase Auth でマジックリンク(メール)ログインを設定。2人分のユーザーを作り `household_members` に登録
3. Next.js プロジェクト作成、Supabase クライアント接続、Vercel へデプロイ
4. 買い物リスト画面(売り場順・タップでチェック・進捗表示)
5. PWA 化(manifest + Service Worker)、オフライン対応、リアルタイム同期
6. **2人のスマホのホーム画面に追加して、実際にスーパーで使ってみる**

### フェーズ2: 在庫

7. 在庫画面(冷蔵/冷凍/常温タブ、+/- 操作、期限警告)
8. 買い物リストのチェック済みを在庫へ流し込む導線(帰宅後に「買った分を在庫に入れる」1タップ)
9. リストに手で品目を足す入力欄(セール品を入れる用)

### フェーズ3: レシートと家計簿

10. `schema_kakeibo.sql` と `seed_kakeibo.sql` を実行(取引155件・分類辞書67件が入る)
11. レシート撮影 → Claude API で品目・金額を読み取り → 確認画面
12. 確認後、**1回の操作で在庫と支出の両方に登録**(receipt_items が両者を橋渡しする)
13. 支出サマリー画面(今月の費目別、1食あたりコスト)

### フェーズ4以降(あとで判断)

14. Cowork の jisui / kakeibo スキルを Supabase 対応に書き換え(チャットとアプリのDB一本化)
15. 献立提案・レシピ表示
16. 冷凍弁当バッチ、月次の振り返り

**フェーズ1を最短で動かすことを優先する。** 使ってみて初めて分かる使いにくさが必ずあるので、全部作ってから直すより早い。

---

## 7. 気をつけること

- **Claude API キーは絶対にクライアントへ出さない。** Route Handler の中だけで使う
- Supabase の `anon key` は公開前提。**守りは RLS で行う**(この設計書のschema.sqlに含めてある)
- レシート画像には店名・日時が写る。Supabase Storage は**非公開バケット**にし、署名付きURLで読む
- 無料枠の条件は変わるので、始める前に Vercel / Supabase / Claude API の現時点の料金を確認する
- **現行の jisui.db は消さずに残す。** 移行が済んで安定するまでのバックアップとして持っておく

---

## 8. Claude Code への最初の指示(コピーして使う)

作業フォルダは **`C:\Users\mmizu\jisui\app`**。ここでClaude Codeを起動する。
データ本体(jisui.db・生成HTML)は一つ上の `jisui\` にあるので、リポジトリには含まれない。
`.gitignore` に `node_modules/` `.next/` `.env*.local` を入れること。

```
自炊管理のWebアプリを作ります。このフォルダにある設計書.md、schema.sql、seed.sql、
schema_kakeibo.sql、seed_kakeibo.sql を全部読んでください。

今回のゴールは設計書のフェーズ1(買い物リスト画面)までです。
家計簿の統合はフェーズ3なので、今は作りません。ただしDB設計だけは
最初から入れておきたいので、Supabaseには4つのSQLをすべて実行します。

技術構成: Next.js App Router + TypeScript + Tailwind + Supabase、Vercelにデプロイ。
PWAにして、スマホのホーム画面から起動できるようにしてください。

特に重視したいのは以下の3点です。
1. スーパーの店内で電波が悪くてもチェックできること(オフライン優先・楽観的更新)
2. 片手で押せること(行全体がタップ領域、高さ56px以上)
3. 夫婦2人で同時に開いたとき、チェックが相手の画面にも即反映されること(Supabase Realtime)

まず全体の実装計画を提示してください。私が確認してから着手してください。
```
