# セットアップ手順(初心者向け)

アプリのコードはもう動く状態にある。ここから先は**ブラウザでの設定**が中心。
所要時間はだいたい40分。焦らず1ステップずつ、「確認」の行まで見てから次へ進む。

用意するもの(すべて無料):

- GitHub アカウント(Supabase と Vercel のログインに使う)
- 妻のメールアドレス

---

## このアプリの仕組み(先にこれを読む)

手順の意味が分かるように、全体像から。**3つのサービスを使う。それぞれ役割が違う。**

| サービス | 役割 | 無いとどうなるか |
|---|---|---|
| **Supabase** | データを預かる場所 | 買い物リストが2人で共有できない |
| **GitHub** | プログラムを保管し、Vercel に渡す場所 | Vercel がプログラムを受け取れない |
| **Vercel** | アプリをインターネットに置いて配る場所 | スマホからアプリを開けない |

データの流れはこうなる。

```
   スマホ / パソコン                    インターネット
 ┌────────────────┐
 │  アプリの画面    │ ←─── アプリ本体を配る ──── [ Vercel ]
 │                │                                 ↑
 │  タップすると    │                          プログラムを渡す
 │       ↓        │                                 │
 │  端末の中に即保存 │                            [ GitHub ]
 │  (IndexedDB)   │
 │       │        │
 └───────┼────────┘
         └──── 電波があるとき同期 ────→ [ Supabase ]
                                          データを保管
                                          2人ぶんを突き合わせる
```

**ここが大事**: タップしたとき、アプリは Supabase の返事を待たない。まず端末の中に書いて画面を更新し、あとから送る。だからスーパーの地下で電波が切れてもチェックできる。

手順1〜7は Supabase(データ置き場)の準備。手順8は Vercel(配る場所)の準備。手順9以降が実際の使用。

---

## ログインの方式について(メールは使わない)

このアプリは **メールアドレス + パスワード** でログインする。理由は2つある。

**1. iPhone のホーム画面アプリはメールリンクと噛み合わない。**
iOS では、ホーム画面に置いたアプリが Safari とは**別の保管庫**を持つ(Cookie・保存データが分離)。
メールのリンクを押すと Safari で開くため、ログイン状態は Safari 側に入り、
**ホーム画面アプリはいつまでもログインできない**。パスワードなら、アプリの中で直接入力できる。

**2. Supabase の標準メール送信は制限が強い。**
公式ドキュメントによれば、**チームメンバー以外のアドレスには配信を拒否**し、**1時間に2通まで**。
無料で無制限に送れると迷惑メールの発射台にされるため、テスト用の最低限に絞られている。
パスワード方式ならメールを一切使わないので、この制限とは無関係になる。

> このおかげで、**妻を Supabase のチームに招待する必要も無くなった**(旧・手順3-1)。
> ユーザーを作って世帯に登録するだけでよい。

---

## 手順1. Supabase のプロジェクトを作る(5分)

> **何のための操作か**
> データを置く場所を用意する。今まで買い物リストは「パソコンの中の jisui.db」にあり、
> パソコンを閉じていると書き込めなかった。それをインターネット上に移して、
> **スマホからも、妻の端末からも、いつでも読み書きできる状態にする**のがこの手順。
> ここで作る「プロジェクト」が、データベース1個分の入れ物になる。

1. https://supabase.com を開く → 右上 **Sign in** → **Continue with GitHub**
2. **New project** を押す
3. 入力する
   - **Name**: `jisui`
   - **Database Password**: 自動生成でよい。**必ずどこかに控える**(後で必要になることがある)
   - **Region**: `Northeast Asia (Tokyo)` を選ぶ

> **なぜ Tokyo を選ぶのか**: データの置き場所が物理的に遠いほど、通信の往復に時間がかかる。
> アメリカのサーバーだと1回の読み書きに0.2秒ほど余計にかかる。スーパーで急いでいるときに効いてくる。

4. **Create new project** → 2〜3分待つ

**確認**: 画面上部の「Setting up project...」が消えて、ダッシュボードが表示される。

---

## 手順2. テーブルを作る(10分)

> **何のための操作か**
> できたばかりのデータベースは**空っぽの倉庫**で、棚が1つもない。
> 「買い物リストという棚には、品名・数量・売り場・買ったかどうかを入れる」という**棚の設計図**を
> 流し込むのがこの手順。あわせて、今 jisui.db に入っている在庫やレシピも引っ越させる。
>
> SQL は「データベースへの命令文」。ファイルを貼って Run を押すと、その命令がまとめて実行される。

`C:\Users\mmizu\jisui\app` にある SQL ファイルを、**この順番で**5回貼り付けて実行する。

| 順 | ファイル | 何が入るか |
|---|---|---|
| 1 | `schema.sql` | テーブルの箱と、他人に見せないための設定(RLS) |
| 2 | `seed.sql` | 今のデータ(器具11・常備品32・在庫18・レシピ6・買い物リスト6 ほか) |
| 3 | `patch_members.sql` | 相手の名前を表示できるようにする(任意) |
| 4 | `schema_kakeibo.sql` | 家計簿のテーブル(使うのはフェーズ3。今は箱だけ) |
| 5 | `seed_kakeibo.sql` | 取引155件・分類辞書67件 |
| 6 | **`patch_views_rls.sql`** | **必須。** 未ログインでも家計データが読めた穴をふさぐ |
| 7 | `schema_v2.sql` | 予定・家事・予算・資産・収入 |
| 8 | `schema_v3.sql` | 予定のラベル・繰り返し・やりとり |
| 9 | `schema_v4.sql` | 資産の内訳・保有銘柄・監視銘柄・俸給表・ローン予定・やること |
| 10 | `seed_v4.sql` | 家計簿アプリから移してきたデータ |
| 11 | **`schema_v5.sql`** | 予定のタグと**非公開の予定**・やることの親子と繰り返し・予定の場所/URL/持ち物/通知 |

> **なぜ順番を守るのか**: 後のファイルが前のファイルの結果を使っている。
> 例えば `seed.sql` は「buy_list という棚に品物を入れろ」という命令だが、
> 棚を作る `schema.sql` を先に流していないと、入れる場所が無くて失敗する。
>
> **なぜ家計簿(4・5)も今入れるのか**: 使うのはフェーズ3だが、後から棚を足すより
> 最初にまとめて作るほうが手間が少ないため。今は箱があるだけで、アプリからは触らない。

やり方(1ファイルにつき1回):

1. Supabase の左メニュー **SQL Editor** → **New query**
2. SQL ファイルをメモ帳などで開き、**Ctrl+A で全選択 → Ctrl+C**
3. SQL Editor に **Ctrl+V** で貼り付け
4. 右下の **Run** を押す(または Ctrl+Enter)
5. 下に `Success. No rows returned` と出れば成功。次のファイルへ

> **`No rows returned`(0行)が成功なのはなぜか**: 棚を作る命令や、データを入れる命令は
> 「何かを取り出す」わけではないので、返ってくる行が無いのが正常。
> 逆に `select`(取り出す命令)を書いたときだけ表が返る。

**確認**: 左メニュー **Table Editor** → `shopping_list` を開くと6行入っている。

うまくいかないとき:

- `relation "households" does not exist` → 順番が違う。`schema.sql` からやり直す
- `already exists` → **すでに作られている**。そのファイルは実行済みなので次へ進んでよい
- 途中で失敗した場合、PostgreSQL は**貼り付けた全体を1つのまとまり**として扱い、
  1つでも失敗すると全部取り消す。だから**そのファイルの先頭から貼り直せばよい**
  (中途半端に入ることはない)

---

## 手順3. 2人分のユーザーを作る(10分)

> **何のための操作か**
> 「誰がアプリを使っているか」を Supabase に登録する。
> これが無いと、アプリを開いた人が夫なのか妻なのか、まったくの他人なのかを区別できない。
> 買い物リストに「誰がチェックしたか」を出せるのも、この登録があるから。

1. 左メニュー **Authentication** → **Users** → **Add user** → **Create new user**
2. メールアドレスとパスワードを入れ、**Auto Confirm User をオン**にして作成
3. 同じ手順でもう1人分も作る

> **ここで決めたパスワードが、そのままアプリのログインに使う。** 忘れないよう控えておく
> (スマホのパスワード管理に保存させるのが確実。ログイン画面は自動入力に対応させてある)。
>
> **Auto Confirm User とは**: 通常は「本当にこのアドレスの持ち主か」を確認するメールが飛ぶ。
> このスイッチを入れると確認を省いて即使える状態にする。自分たちのアドレスだと分かっているので省いてよい。
> **オフにすると、確認メールが届くまでログインできない**ので必ずオンにする。

> **相手のメールアドレスに、こちらから何かを送る必要はない。**
> このアプリでメールアドレスは**ログインIDとして使う文字列**であって、連絡先としては一度も使わない。
> 確認メールは Auto Confirm で省略され、ログインリンクはパスワード方式なので存在しない。
> 相手に伝えるのは「アドレスとパスワードの組み合わせ」だけでよい。
>
> **パスワードを忘れたときは、管理者(自分)が Authentication → Users から再設定する。**
> 再設定メールは使えない(チームメンバー宛にしか届かないため)。
> 相手の端末を預からなくてもパソコンだけで直せるので、実用上は困らない。

**確認**: Users の一覧に2人並ぶ。

---

## 手順4. 2人を「わが家」に登録する(5分)

> **何のための操作か**
> **ここがこのアプリで一番大事な仕組み。**
>
> データベースには「わが家」という世帯が1つあり、買い物リストも在庫も、すべて
> 「どの世帯のものか」という印が付いている。そして Supabase には
> **RLS(行レベルセキュリティ)** という守りが設定してあり、
> 「**自分が所属する世帯のデータしか見せない**」という判定を毎回行う。
>
> この手順は、その判定に使う名簿に自分たちを書き込む作業。
> 名簿に載っていないと、ログインは成功するのに**リストが1件も見えない**。
> 故障に見えるが、RLS が正しく働いている結果でもある。
>
> **ここを飛ばすのが一番多い失敗。**

1. **Authentication → Users** で、1人目の行をクリック
2. **User UID**(`a1b2c3d4-...` のような文字列)をコピー
3. 2人目も同様にコピー
4. **SQL Editor** で新しいクエリを開き、下を貼る。
   - **``` の行は貼らない**(Markdown の飾りで、SQL ではない)
   - UID は**必ずシングルクォート `'` で囲む**。囲み忘れが一番多い間違い

```sql
insert into household_members (household_id, user_id, display_name) values
  ('00000000-0000-4000-8000-000000000001', '9f6e23dc-4188-4c98-bdf9-6aeef357b448', '夫'),
  ('00000000-0000-4000-8000-000000000001', 'd7b55fc1-1589-4006-96cb-d1857fe33051', '妻');
```

> **クォートが要るのはなぜか**: SQL では `'...'` で囲まれたものが「文字の値」、
> 囲まれていないものは「列名や命令」として読まれる。囲まないと
> `9f6e23dc-4188-...` が引き算の式と解釈されてエラーになる。
>
> **`00000000-0000-4000-8000-000000000001` は何か**: 「わが家」を指す世帯の番号。
> `seed.sql` が作ったもので、決め打ちの値。ここは書き換えない。

5. **Run**

**確認**: 下のSQLを実行して2行返ってくる。

```sql
select display_name, user_id from household_members;
```

---

## 手順5. (パスワード方式では不要)

メールのリンクでログインする方式のときは、「リンクを押した後どこへ戻すか」を
Supabase に登録する必要があった(Redirect URLs)。
**パスワード方式ではメールを経由しないので、この登録は要らない。**

すでに登録してあっても害はないので、そのままでよい。

> **参考: なぜ許可制だったのか**
> 戻り先を自由に指定できると、「Supabase のログインリンクだが、戻り先は攻撃者のサイト」という
> メールを作れてしまい、押した人のログイン情報が攻撃者に渡る。それを防ぐため、
> 持ち主が事前に登録したURLにしか返さない決まりになっている。

---

## 手順6. 鍵をアプリに渡す(5分)

> **何のための操作か**
> アプリに「どの Supabase プロジェクトに、どの鍵でつなぐか」を教える。
> この2つを渡すまで、アプリは接続先を知らないので**ローカルモード**(端末の中だけ)で動く。

1. 左下の歯車 **Project Settings** → **API Keys**
2. **Publishable key**(`sb_publishable_...` で始まる)をコピー
   - 見当たらなければ **Create new API keys** を押す
   - 古い形式でもよい。その場合は **Legacy API Keys** タブの `anon` `public` の値

> **この鍵は人に見られてよいのか?** — よい。**設計上そうなっている。**
> この鍵はアプリの中に埋め込まれて配られるので、どうやっても隠せない。
> だから Supabase は「鍵は入口を開けるだけ。中で何が見えるかは RLS が決める」という作りにしている。
> 手順4で名簿に載っていない人は、この鍵を持っていても**何も見えない**。
> 実際、ログイン前にこの鍵でデータを取りにいくと、空っぽ(`[]`)が返る。
>
> ただし **Secret key(`sb_secret_...`)や service_role は別物**。こちらは RLS を無視して
> 全部見えてしまうので、**絶対に外に出さない**。アプリにも書かない。

3. 同じ設定画面の **Data API**(または API)にある **Project URL**
   - **`https://なにか.supabase.co` まで。`/rest/v1/` は付けない**
   - 画面に出ているのは呼び出し用のURLなので、末尾を削ること

> **なぜ末尾を削るのか**: アプリ側が `/rest/v1/...` の部分を自分で組み立てる。
> 付けたままだと `/rest/v1/rest/v1/...` という存在しない住所になり、つながらない。

4. `C:\Users\mmizu\jisui\app` に **`.env.local`** という名前のファイルを作り、下を書いて保存

```
NEXT_PUBLIC_SUPABASE_URL=https://quxmfeyobiiuyofqvaei.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=（コピーした鍵をそのまま貼る）
```

> `sb_publishable_` は**鍵に最初から付いている**。上の行に重ねて書かないこと。

> **なぜプログラムに直接書かず、別ファイルにするのか**
> プログラムは GitHub にアップロードする。もし鍵をプログラムに直接書くと、鍵も一緒に上がってしまう。
> `.env.local` は **Git に入れない設定**にしてあるので、鍵はこのパソコンから出ない。
> そのかわり、本番用には Vercel に**同じ値をもう一度登録する**(手順8-3)。
> 同じことを2回入力するのは無駄に見えるが、「鍵をプログラムと分けて持つ」ための仕組み。

5. 開発サーバーを**再起動**する(起動中なら Ctrl+C で止めてから)

```
npm run dev
```

> **なぜ再起動が要るのか**: 環境変数は**起動したときに一度だけ**読み込まれる。
> 動かしたままファイルを書き換えても、アプリは古い状態(未設定)のままになる。

**確認**: http://localhost:3000 を開くと、これまでと違って**ログイン画面**が出る。
紫色の「ローカルモード」バッジが消えていれば、Supabase につながっている。

---

## 手順7. ログインしてみる(3分)

> **何のための操作か**
> ここまでの設定(棚・名簿・鍵)が**全部つながっているか**を確かめる総合テスト。
> リストが6件出れば、認証も RLS も世帯の紐付けも正しいと分かる。

1. ログイン画面に、手順3で決めた**メールアドレスとパスワード**を入れる
2. **ログイン**を押す
3. 買い物リストが表示される

> ログイン状態は**その端末のそのアプリ**に保存される。次からは開くだけで入れる。
> パソコンとスマホは別々に1回ずつログインが必要(端末をまたいで共有されないため)。

**確認**: リストに6件出る。「ローカルモード」バッジが無い。
品目をタップすると、右側に小さく **`自分`** と時刻が出る(`checked_by` が記録された印)。

出ないとき:

| 症状 | 原因と対処 |
|---|---|
| リストが空っぽ | 手順4の登録漏れ。`select * from household_members;` で確認 |
| 「メールアドレスかパスワードが違います」 | 打ち間違い。分からなくなったら Authentication → Users から再設定できる |
| 「このユーザーはまだ確認が済んでいません」 | 手順3で **Auto Confirm User** がオフだった。Users から確認済みにする |
| 「ローカルモード」が出たまま | 手順6の環境変数。本番なら Vercel 側の登録漏れ |

---

## 手順8. インターネットに公開する(15分)

> **何のための操作か**
> ここまでのアプリは `localhost` = **このパソコンの中だけ**で動いている。
> パソコンの電源を切れば止まるし、スマホからは開けない。
> スーパーで使うには、**常に動いている場所**にアプリを置く必要がある。それが Vercel。
>
> 流れは「パソコン → GitHub → Vercel」。GitHub を経由するのは、Vercel が
> 「GitHub の内容が変わったら自動で作り直して配る」という仕組みだから。
> 一度つないでおけば、次からは**変更を GitHub に上げるだけで本番に反映**される。

### 8-1. Git に入れる(コミット)

`C:\Users\mmizu\jisui\app` でコマンドを実行:

```
git init
git add -A
git commit -m "jisui phase 1"
```

> **コミットとは**: 今のファイル一式に「この時点の記録」という札を付けて保存すること。
> 写真を1枚撮るのに近い。あとで「動いていた頃に戻す」ができるようになる。
> `git init` は最初の1回だけ。2回目以降は `add` と `commit` だけでよい。

### 8-2. GitHub に上げる

1. https://github.com/new を開く
2. **Repository name**: `jisui`、**Private** を選ぶ

> **必ず Private にする理由**: Public にすると世界中の誰でも中身を読める。
> 鍵そのものは入っていないが、家族の食事の記録・買い物の内容・在庫が丸見えになる。
> 個人の生活記録なので、非公開にしておく。

> **「Add a README file」にはチェックを入れない**: 入れると GitHub 側にも履歴ができてしまい、
> パソコン側の履歴とぶつかって、アップロードが拒否される。空の状態で作るのが正しい。

3. 作成後、パソコンのターミナルで次を実行(`mizuno1224` は自分のユーザー名)

```
git remote add origin https://github.com/mizuno1224/jisui.git
git push -u origin main
```

> `remote add` は「上げ先の住所を登録する」、`push` は「実際に送る」。
> 初回は認証を求められるので、ブラウザが開いたら承認する。

### 8-3. Vercel に載せる

1. https://vercel.com → **Continue with GitHub**
2. **Add New... → Project** → `jisui` を **Import**
3. **Environment Variables** に、手順6の2つを登録する
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`

> **ここでもう一度入れるのはなぜか**: `.env.local` は GitHub に上げていないので、
> Vercel はこの2つの値を知らない。登録しないと、本番のアプリが
> 接続先を知らないまま**ローカルモードで動いてしまう**。
> Key の欄に `.env.local` の中身をそのまま貼ると、2つに自動で分かれる。

4. **Deploy** → 2〜3分待つ

> **この2〜3分に何が起きているか**: Vercel が GitHub からプログラムを取り寄せ、
> ブラウザが読める形に変換(ビルド)し、世界中に配れる状態に置いている。

5. 発行されたURL(`https://jisui-xxxx.vercel.app`)をメモ

### 8-4. 本番URLを Supabase に登録する

> **これを忘れるとログインできない。** 手順5で登録したのは `localhost`(自分のパソコン)だけ。
> 本番URLは Supabase から見れば「知らない戻り先」なので、弾かれる。

パスワード方式なので、**Redirect URLs の登録は不要**(手順5参照)。

覚えておくのは本番URLだけ。

```
https://jisui.vercel.app
```

> Vercel は1回のデプロイで3つのURLを発行するが、**`jisui.vercel.app` が本番用**。
> 残りは自動生成の別名(`jisui-git-main-...` など)なので、普段は使わない。
> ホーム画面に置くのもこれ。

**確認**: 本番URLを開いてログインでき、リストが出る。

> **パソコンでログイン済みでも、本番URLでは再度ログインが必要。**
> `localhost`(自分のパソコン)と `jisui.vercel.app` は別の場所として扱われ、
> ログイン状態は共有されない。

---

## 手順9. スマホのホーム画面に置く(3分)

> **何のための操作か**
> ブラウザで開くと、上下にアドレスバーやタブが残って画面が狭い。
> 「ホーム画面に追加」すると、**普通のアプリと同じ全画面**で起動し、アイコンから1タップで開ける。
> さらに、この操作をすると裏でアプリ本体が端末に保存され、**電波が無くても起動できる**ようになる。
>
> App Store への申請が要らないのは、これが「アプリのように振る舞うWebページ(PWA)」だから。
> 審査も更新作業も無く、こちらが直せば次に開いたときには新しくなっている。

**iPhone**: **Safari** で `https://jisui.vercel.app` を開く
→ 下の共有ボタン(□に↑)→ **ホーム画面に追加**

> **普段 Chrome を使っていても、追加の操作だけは Safari で行うのが確実。**
> iPhone の Chrome にも「ホーム画面に追加」がある場合はそれでもよいが、
> Safari のほうが確実に全画面(standalone)で開く。
>
> どちらで追加しても、**ホーム画面アプリは独立した1つのアプリ**になる。
> 普段のブラウザが Chrome か Safari かは、この後まったく関係しない。

**Android**: Chrome で開く → 右上の︙ → **アプリをインストール**

### 追加したら、アイコンから開いて**もう一度ログインする**

> **これは異常ではない。** iOS のホーム画面アプリは、Safari とも Chrome とも
> **別の保管庫**を持つ(Cookie・保存データが分離されている)。
> ブラウザでログイン済みでも、ホーム画面アプリから見れば初回になる。
>
> パスワード方式にしたのは、まさにここで詰まらないため。
> アプリの中で直接入力できるので、1回入れれば以後は開くだけになる。
> スマホのパスワード管理に保存させておくと、次に必要になったとき楽。

**確認**: ホーム画面のアイコン(緑のチェックマーク)から起動すると、
アドレスバーの無い全画面で開き、リストが出る。

妻のスマホでも同じことをする(妻のメールアドレスとパスワードでログイン)。

---

## 手順10. 本番で確かめる

> **何のための操作か**
> 設計書で「これができないと意味がない」とした3つが、実際に動くかを確かめる。
> スーパーで初めて試して駄目だった、という事態を避けるため。

1. **2人同時に開く** → 片方がチェックすると、もう片方の画面も数秒で変わる
   (→ 手分けして買っても、買い忘れと重複が起きない)
2. **機内モードにする** → チェックできる。上部に「オフライン」「未送信 n」が出る
   (→ 店内で電波が切れても操作が消えない。「未送信」は送信待ちの件数)
3. **機内モードを解除** → 「未送信」が消える。相手の画面にも反映される
   (→ 電波が戻れば自動で送られる。何もしなくてよい)

ここまで確認できたら、実際に買い物で使ってみる。
使ってみて分かった不便は、そのまま次の改修の材料になる(設計書 6-フェーズ1-6)。

---

## 用語のまとめ

| 言葉 | かみくだくと |
|---|---|
| **データベース** | データを整理して置いておく倉庫 |
| **テーブル** | 倉庫の中の棚。「買い物リスト」「在庫」など種類ごとにある |
| **SQL** | データベースへの命令文。「作れ」「入れろ」「取り出せ」 |
| **RLS** | 「自分の世帯のデータしか見せない」という守り。鍵が漏れても中身は守られる |
| **環境変数** | 接続先や鍵など、プログラム本体と分けて持つ設定値 |
| **コミット** | 今のファイル一式に「この時点」という札を付けて保存すること |
| **デプロイ** | 作ったアプリをインターネット上に配置して、使える状態にすること |
| **PWA** | アプリのように振る舞う Web ページ。ホーム画面に置け、オフラインでも動く |
| **IndexedDB** | ブラウザが端末の中に持っている保存場所。電波が無いときの控え |

---

## 参考

- [Supabase: API キーの新旧について](https://supabase.com/docs/guides/getting-started/api-keys)
- [Supabase: 標準メール送信の制限とカスタムSMTP](https://supabase.com/docs/guides/auth/auth-smtp)
